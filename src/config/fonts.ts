import { Inter } from "next/font/google";

export const fontInter = Inter({
	subsets: ["latin"],
	variable: "--font-inter",
});

export const fontMono = Inter({
	subsets: ["latin"],
	variable: "--font-mono",
	weight: "400",
});
